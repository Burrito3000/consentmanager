{
  "name": "@dpdp-cmp/consent-sdk",
  "version": "0.1.0",
  "description": "DPDP Consent Management SDK — embeddable consent banner",
  "main": "dist/cmp.js",
  "types": "dist/cmp.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "lint": "eslint src/",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "rollup": "^4.22.0",
    "@rollup/plugin-typescript": "^12.0.0",
    "@rollup/plugin-terser": "^0.4.0",
    "tslib": "^2.7.0",
    "vitest": "^2.1.0",
    "eslint": "^8.57.0"
  }
}
